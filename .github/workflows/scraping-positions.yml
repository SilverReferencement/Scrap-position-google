name: Scraping Positions Google

on:
  # Exécution 3 fois par mois : les 5, 15 et 25 de chaque mois à 6h00 (heure de Paris)
  schedule:
    - cron: '0 5 5 * *'   # Le 5 de chaque mois à 5h UTC (6h Paris hiver / 7h Paris été)
    - cron: '0 5 15 * *'  # Le 15 de chaque mois à 5h UTC (6h Paris hiver / 7h Paris été)
    - cron: '0 5 25 * *'  # Le 25 de chaque mois à 5h UTC (6h Paris hiver / 7h Paris été)

  # Exécution manuelle à la demande
  workflow_dispatch:

jobs:
  scrape-positions:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout du code
        uses: actions/checkout@v4

      - name: 🟢 Installation de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📦 Installation des dépendances
        run: npm ci

      - name: 🔑 Configuration des credentials Google
        run: |
          echo '${{ secrets.GOOGLE_CREDENTIALS }}' > credentials.json

      - name: ⚙️ Configuration de l'environnement
        run: |
          echo "SPREADSHEET_ID=${{ secrets.SPREADSHEET_ID }}" >> .env
          echo "SHEET_NAME=${{ secrets.SHEET_NAME }}" >> .env
          echo "SERPAPI_KEY=${{ secrets.SERPAPI_KEY }}" >> .env
          echo "SERPAPI_KEY2=${{ secrets.SERPAPI_KEY2 }}" >> .env
          echo "SERPAPI_KEY3=${{ secrets.SERPAPI_KEY3 }}" >> .env
          echo "SERPAPI_KEY4=${{ secrets.SERPAPI_KEY4 }}" >> .env
          echo "TARGET_DOMAIN=fix-my-kea.com" >> .env

      - name: 🚀 Exécution du scraping avec SerpApi
        run: npm start
        env:
          NODE_ENV: production

      - name: 🧹 Nettoyage des credentials
        if: always()
        run: |
          rm -f credentials.json
          rm -f .env

      - name: ✅ Notification de succès
        if: success()
        run: |
          echo "✅ Scraping terminé avec succès!"
          echo "📊 Vérifiez votre Google Sheet pour les résultats."

      - name: ❌ Notification d'échec
        if: failure()
        run: |
          echo "❌ Le scraping a échoué."
          echo "📝 Consultez les logs ci-dessus pour plus de détails."
